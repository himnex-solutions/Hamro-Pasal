import 'package:flutter_test/flutter_test.dart';

import 'package:hamro_pasal/main.dart';

void main() {
  test('app widget can be constructed', () {
    expect(const HamroPasalApp(), isA<HamroPasalApp>());
  });
}
